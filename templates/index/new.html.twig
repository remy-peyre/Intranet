{% extends 'index.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_TEACHER') %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    {% endif %}

{% endblock %}
